import { SimpProdsPipe } from './simp-prods.pipe';

describe('SimpProdsPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpProdsPipe();
    expect(pipe).toBeTruthy();
  });
});
